# Movie Reviews API

## Overview

This RESTful API allows you to manage movie reviews. You can perform CRUD (Create, Read, Update, Delete) operations on movie reviews. The API is built using Node.js, Express, and MongoDB.

## Base URL

The base URL for accessing the API is:
